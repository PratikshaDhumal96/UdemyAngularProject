import { Inless2Directive } from './inless2.directive';

describe('Inless2Directive', () => {
  it('should create an instance', () => {
    const directive = new Inless2Directive();
    expect(directive).toBeTruthy();
  });
});
